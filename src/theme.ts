import { createTheme } from '@mantine/core'

export const theme = createTheme({
  fontFamily: 'NationalPark, sans-serif',
  fontFamilyMonospace: 'Mononoki, monospace',
  // components: {
  //   TextInput: TextInput.extend({
  //     vars: (theme, props) => ({
  //       styles: {
  //         input: { height: 16, minHeight: 16 },
  //       },
  //     }),
  //   }),
  // },
})
